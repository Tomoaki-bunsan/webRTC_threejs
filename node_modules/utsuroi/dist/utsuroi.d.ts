declare module "action" {
    import { AnimationAction } from 'three/src/animation/AnimationAction';
    export class Action {
        private actionData;
        private name;
        private weight;
        constructor(actionData: AnimationAction);
        getName(): string;
        getWeight: () => number;
        setWeight(val: number): void;
        setLoop: (val: boolean) => void;
        play(): void;
        reset(): void;
    }
}
declare module "manipulator" {
    import THREE from 'three';
    import EventEmitter from 'eventemitter3';
    export class Manipulator extends EventEmitter {
        private clock;
        private mixer;
        private animationEnabled;
        private actions;
        private currentAction?;
        get currentActionName(): string | undefined;
        constructor(scene: THREE.Scene, animations: THREE.AnimationClip[]);
        private findAction;
        to(actionName: string, duration: number, loop?: boolean): void;
        play(actionName: string, loop?: boolean): void;
        pause(): void;
        update(): void;
    }
}
declare module "utsuroi" {
    export * from "manipulator";
    export * from "action";
}
